name: Build APK

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            ember/target
          key: ${{ runner.os }}-ember-cargo-${{ hashFiles('ember/**/Cargo.lock', 'ember/Cargo.lock') }}

      - name: Install cargo-ndk
        run: cargo install cargo-ndk

      - name: Setup Android NDK
        id: ndk
        uses: nttld/setup-ndk@v1
        with:
          ndk-version: r26d

      - name: Add Android targets
        run: rustup target add aarch64-linux-android armv7-linux-androideabi

      - name: Build Rust library
        working-directory: ember
        env:
          ANDROID_NDK_HOME: ${{ steps.ndk.outputs.ndk-path }}
        run: |
          mkdir -p android/app/src/main/jniLibs
          cargo ndk -t arm64-v8a -t armeabi-v7a -o android/app/src/main/jniLibs build -p ember-client --features android --release

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Create signing keystore
        run: |
          mkdir -p ember/android
          keytool -genkey -v -keystore ember/android/ember.keystore -alias ember -keyalg RSA -keysize 2048 -validity 10000 \
            -storepass android -keypass android -dname "CN=Ember, OU=Dev, O=Ember, L=City, S=State, C=US"

      - name: Build APK
        working-directory: ember/android
        env:
          ANDROID_KEYSTORE_PATH: ${{ github.workspace }}/ember/android/ember.keystore
          ANDROID_KEYSTORE_PASSWORD: android
          ANDROID_KEY_ALIAS: ember
          ANDROID_KEY_PASSWORD: android
        run: chmod +x gradlew && ./gradlew assembleRelease

      - name: Rename APK to ember.apk
        run: |
          APK=$(ls ember/android/app/build/outputs/apk/release/app-release*.apk 2>/dev/null | head -1)
          cp "$APK" ember.apk

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: ember-apk
          path: ember.apk

      - name: Create Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: ember.apk
          generate_release_notes: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
